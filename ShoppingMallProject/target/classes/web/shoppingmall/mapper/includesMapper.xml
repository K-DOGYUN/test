<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "=//mybatis.org//DTD Mapper 3.0//EN"
	"http:/mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace = "web.shoppingmall.mapper.includesMapper">
	<select id="readLargeCategory" resultType = "String">
		SELECT DISTINCT large_category FROM category ORDER BY large_category
	</select>
	<select id="readMediumCategory" resultType = "String">
		SELECT DISTINCT Medium_category
		FROM category
		Where Large_category = #{largeCategory}
		ORDER BY Medium_category
	</select>
	<select id="readSmallCategory" resultType = "String">
		SELECT DISTINCT small_category
		FROM category
		Where medium_category = #{mediumCategory}
		ORDER BY small_category
	</select>
	<select id="readDetailCategory" resultType = "String">
		SELECT DISTINCT detail_category
		FROM category
		Where small_category = #{smallCategory}
		ORDER BY detail_category
	</select>
</mapper>